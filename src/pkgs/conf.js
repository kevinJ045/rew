const fs = require('fs');
const path = require('path');

const CONFIG_PATH = path.resolve(process.env.HOME, '.config');

const createPackageRoot = (packageName) => {
  
}

module.exports = (context) => ({
  create: (packageName) => {

  }
});